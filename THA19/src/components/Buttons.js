import Button from "./Button"

const Buttons = () => {
    return (
        <div className="buttons">
            <Button/>
            <Button/>
            <Button/>
            <Button/>
        </div>
    )
}

export default Buttons
